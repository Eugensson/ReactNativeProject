import React from "react";
import { Text, StyleSheet } from "react-native";

const Map = () => {
  return <Text style={styles.container}>Map Screen</Text>;
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
});

export default Map;
